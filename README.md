# Telegram Bot для отслеживания подписок

Этот бот позволяет отслеживать ваши подписки на различные сервисы, включая стоимость, валюту и дату обновления.

## Основные функции

- **Поэтапное добавление подписок** с выбором сервиса, валюты, стоимости и даты обновления
- Просмотр списка всех подписок с отображением валюты
- Удаление подписок по ID
- Подсчет общей стоимости подписок по каждой валюте
- Ежедневные уведомления о предстоящих платежах
- Интерактивные кнопки для удобного взаимодействия

## Популярные сервисы для подписок

- Яндекс.Плюс (Yandex.Plus)
- Netflix
- Spotify
- YouTube Premium
- Amazon Prime
- Disney+
- Apple Music
- Microsoft 365
- Adobe Creative Cloud

## Команды

- `/start` - Начало работы с ботом
- `/add` - Добавить новую подписку (поэтапно)
- `/list` - Показать все подписки
- `/delete` - Удалить подписку
- `/total` - Показать общую сумму расходов по валютам
- `/help` - Показать справку

## Кнопки

- ➕ **Добавить подписку** - Поэтапное добавление новой подписки
- 📋 **Мои подписки** - Показать все ваши подписки
- ❌ **Удалить подписку** - Удалить подписку по ID
- 💰 **Общая сумма** - Показать общую сумму расходов
- ❓ **Помощь** - Показать это сообщение снова

## Установка и запуск

1. Установите зависимости:
   ```
   pip install -r requirements.txt
   ```

2. Получите токен у [@BotFather](https://t.me/BotFather) в Telegram

3. Установите переменные окружения:
   ```
   export TELEGRAM_BOT_TOKEN='ваш_токен'
   export MYSQL_HOST='mainline.proxy.rlwy.net'
   export MYSQL_PORT='15324'
   export MYSQL_USER='root'
   export MYSQL_PASSWORD='ваш_пароль'
   export MYSQL_DATABASE='railway'
   ```

4. Запустите бота:
   ```
   python bot.py
   ```

## Развертывание на Railway

1. Создайте аккаунт на [railway.app](https://railway.app/)
2. Создайте новый проект
3. Подключите репозиторий с ботом или загрузите файлы напрямую
4. Установите переменные окружения в настройках проекта:
   - `TELEGRAM_BOT_TOKEN` - токен бота
   - `MYSQL_HOST` - хост базы данных
   - `MYSQL_PORT` - порт базы данных
   - `MYSQL_USER` - имя пользователя
   - `MYSQL_PASSWORD` - пароль
   - `MYSQL_DATABASE` - имя базы данных
5. Railway автоматически запустит бота при деплое

## Структура базы данных

База данных MySQL с таблицей `subscriptions`:

- `id` - уникальный идентификатор подписки (AUTO_INCREMENT)
- `user_id` - ID пользователя в Telegram
- `service_name` - название сервиса
- `cost` - стоимость подписки (DECIMAL 10,2)
- `currency` - валюта (USD, EUR, RUB, UAH, KZT, BYN)
- `renewal_date` - дата обновления подписки (DATE)
- `created_at` - дата создания записи (TIMESTAMP)

## Поддерживаемые валюты

- USD ($) - Доллар США
- EUR (€) - Евро
- RUB (₽) - Российский рубль
- UAH (₴) - Украинская гривна
- KZT (₸) - Казахстанский тенге
- BYN (Br) - Белорусский рубль

## Уведомления

Бот отправляет ежедневные уведомления о предстоящих платежах в 9:00 UTC.
Уведомления приходят за 7 дней до даты обновления подписки.